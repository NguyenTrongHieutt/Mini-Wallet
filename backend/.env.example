NODE_ENV=development
PORT=1337

MONGO_URI=mongodb://127.0.0.1:27017/mini_wallet?replicaSet=rs0
MONGO_HOST=127.0.0.1
MONGO_PORT=27017
MONGO_DATABASE=mini_wallet

JWT_SECRET=replace-with-a-long-random-secret
SESSION_SECRET=replace-with-another-long-random-secret
AUTH_TOKEN_TTL_SECONDS=86400
AUTH_COOKIE_NAME=jwt
COOKIE_SECURE=false

DEFAULT_CURRENCY=VND
REGISTRATION_BALANCE=0
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100
TRAIL_TTL_SECONDS=900
POCKET_LOCK_TTL_SECONDS=300
PROVIDER_TIMEOUT_MS=10000

CORS_ORIGIN=http://localhost:3000,http://localhost:5173,http://localhost:8080

# Required by npm run seed:officer and npm run seed:all.
OFFICER_PHONE=0976172613
OFFICER_PASSWORD=Hieu123!@#
# Optional; defaults to the versioned officer fixture display name.
OFFICER_DISPLAY_NAME=
